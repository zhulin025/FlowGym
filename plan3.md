# VibeMove 3.0: 飞书 (Lark) 自驱动与长任场景规划

3.0 版本的核心是将 `VibeMove` 进化为一个能够编排复杂“体力-指令”流的极客工作站。我们将深度集成 **飞书 CLI (lark-cli)**，并引入跨应用的自动化控制（浏览器、编辑器、AI 助手）。

## 一、 核心概念：VibeScript 工作流
不再是简单的“动作 A = 按键 B”，而是“动作序列 + 状态维持 = 业务结果”。

## 二、 重点场景：【AI 行业情报站】 (AI Intelligence Briefing)

这套动作序列大约需要 3-4 分钟，旨在通过物理能量投入完成一次深度的信息采集与发布。

### 阶段 1：信息筛选 (高抬腿 15 次)
- **动作**：**高抬腿 (Knee Up)**。
- **映射**：每次抬腿执行脚本 `lark-cli scroll-browser --down` (利用 AppleScript 模拟浏览器向下滚动一屏)。
- **目标**：在 15 次抬腿过程中，快读扫描今日行业头条。

### 阶段 2：深度采集 (深蹲保持 + 触发听写)
- **动作**：**深蹲保持 (Hold Squat)**。
- **映射**：当处于下蹲状态时，持续按下 **FN**。
- **任务**：你口述当前发现的亮点：“刚才那篇文章提到的多模态架构非常新颖，重点关注”。
- **约束**：必须保持站姿，录入才有效。

### 阶段 3：智能排版 (左右侧弓步 10 次)
- **动作**：**侧弓步 (Side Lunge)**。
- **映射**：在 `Google Chrome`、`Claude.app` 和 `Feishu` 之间循环切换焦点。
- **任务**：将采集的文字交给 AI 总结，并将总结后的 Markdown 文本粘贴进草稿箱。

### 阶段 4：文档发布 (开合跳 20 次 + 一记重拳)
- **动作**：**开合跳 (Jumping Jack)**。
- **映射**：触发进度条 Overlay，显示“正在同步云端文档...”。
- **动作**：**向前冲拳 (Jab)**。
- **指令**：`lark-cli docs create` + `lark-cli im send`。
- **结果**：一封附带总结链接的文档自动分发给项目组。

---

## 三、 技术路线图

1. **新维度动作识别**：
   - 实现 `kneeUp` (高抬腿) 的 Y 轴动态判定。
   - 实现 `jumpingJack` (开合跳) 的肢体夹角判定。
   - 实现 `punching` (动作加速度) 的简单估算。

2. **外部工具桥接**：
   - **LarkHelper**：Swift 调用 `lark-cli` 的子进程管理。
   - **BrowserHelper**：通过 AppleScript 控制 Chrome/Safari 滚动、取词、Tab 切换。

3. **状态持久化与防舞弊**：
   - 引入物理能量槽 (Stamina Bar)，只有真实的、维持性的动作才能换取 AI 任务的“处理进度”。

---
*VibeMove 3.0 - 让每一次汇报都成为一次肌肉的记忆。*
